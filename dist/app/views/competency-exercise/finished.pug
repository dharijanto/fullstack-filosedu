extends ../layout/index.pug
include ../includes/utils.pug

append head
  link(rel="stylesheet" href="/assets/vendor/toastr/toastr.css")
  style.
    .btn {
      margin: 1%;
    }

    #notes {
      font-size: 120%;
      background-color: #ffc800;
      padding: 1%;
    }
    .input-group {
      margin: auto;
    }

append initialization
  - const activePage = 'modules'

block contents
  div.container
    if user
      p= 'Selamat Datang, ' + user.fullName || user.username
    .row.jumbotron.text-center
      .row
        .col-md-12
          img(src="/assets/img/congratulations.png")
        .col-md-12
          p.
            #[h2 Uji Kompetensi Telah Selesai]
            Setelah ini, anda akan mendapatkan nilai uji-kompetensi yang baru diselesaikan. Namun, hasil tersebut tidaklah
            menggambarkan kemampuan putra/putri anda sepenuhnya.
            Untuk mendapatkan analisa yang lebih mendalam, team Filosedu untuk menghubungi anda:
      .row
        .col-md-12
          .input-group
            //label(for='pwd') Nama Lengkap:
            input#input-name.form-control(type='text', placeholder='Nama', aria-describedby='basic-addon1')
          .input-group.text-center
            //label(for='pwd') E-mail:
            input#input-email.form-control(type='text', placeholder='Email', aria-describedby='basic-addon1')
          .input-group.text-center
            //label(for='pwd') Nomor Telepon:
            input#input-phone.form-control(type='text', placeholder='Nomor Telepon', aria-describedby='basic-addon1')
        .col-md-12
          button.btn.btn-primary#btn-contact-us Masukan
          // button.btn.btn-secondary#btn-dont-contact-us Jangan Hubungi Saya

append scripts
  script(src=`${bundle}`)
